DROP-IN PACKAGE (STATIC LIST VERSION)

ADD THESE FILES:
- /product-taxonomy.html
- /product-taxonomy-category.html
- /css/taxonomy.css
- /js/taxonomy.js

ASSUMED EXISTING FILE:
- /style.css (loaded first; unchanged)

LINKS:
- Both pages link to /dashboard.html
  If your dashboard file is named differently, change the href in BOTH HTML files.

BEHAVIOUR:
- Primary page shows category tiles.
- Each tile opens a category page with a static list (no links).
- Back returns to the tiles page.
